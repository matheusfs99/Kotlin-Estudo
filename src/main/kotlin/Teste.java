public class Teste {
    public void ternario(){
        int valor = 10;

        // Operador ternário
        String str = valor == 10 ? "Sim": "Não";

        String a;
        if(valor == 10)
            a = "Sim";
        else
            a = "Não";
    }

    public void meuNome(){
        String str = null;
        str.length();
    }
}
